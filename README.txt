######README######

To install dependencies: navigate to the project folder, then in your python terminal run:

pip install  -r requirements.txt 

OR

pip install tensorflow scikit-learn numpy matplotlib Pillow seaborn opencv-python


To run the models:

run main.py

In the terminal, enter the number corresponding to the model you want to run
results will be output to the terminal, and graphs to the plots page / display out
